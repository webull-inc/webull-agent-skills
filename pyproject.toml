[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "webull-openapi-skills"
version = "0.1.0"
description = "Webull OpenAPI Skill built on the official Python SDK"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

dependencies = [
    "webull-openapi-python-sdk==2.0.3",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "requests>=2.28",
]

[project.optional-dependencies]
dev = [
    "hypothesis>=6.0",
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[project.scripts]
webull-skill = "webull_openapi_skills.scripts.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["webull_openapi_skills"]

[tool.pytest.ini_options]
testpaths = ["webull_openapi_skills/tests"]
